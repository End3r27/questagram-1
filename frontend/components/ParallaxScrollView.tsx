import React from 'react';
import { ScrollView, ScrollViewProps } from 'react-native';

export default function ParallaxScrollView(props: ScrollViewProps) {
  // Placeholder: just a ScrollView for now
  return <ScrollView {...props} />;
}